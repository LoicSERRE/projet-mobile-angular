import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'petit_prince',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
